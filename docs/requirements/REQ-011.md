# REQ-011: Console Vite paint + per-item Hide (Issue #27)

- Intent: Fix the exit-week skeptic FAIL so Console on :8802 paints a real UI (not a black main pane) with health reporting the live Vite path as `static:false`, and deliver per-item Hide from sidebar (true hide, not archive) plus a Welcome-gate that does not sticky-block paint.
- Success:
  1. Headed Edge (tall enough for unique URL-bar shots, no --app, >660x500) against live :8802 shows URL bar + painted sidebar/main (not black pane); screenshot hash unique vs prior FAIL pack `{2aa9cd02, 6fb8edbb, e7619831}`.
  2. `GET http://127.0.0.1:8802/api/health` returns JSON with `"static":false` while Vite is the live Console SoT.
  3. Live Sidebar exposes per-item "Hide from sidebar" / Unhide + Hidden section (not bulk-only HideSidebarBotsToggle / HideInterBotToggle). True hide, not `archiveBot` toasts. Do not claim Hide PASS from the engineering session alone.
  4. PR #24 (or successor) leaves draft only after URL-bar QA PASS; conflicts resolved; no squash-merge without Matthew; PR includes `Fixes #27`.
- Constraints: Eng SoT `C:\OpenMausBot-src`. Do not kill live Vite :8802 PID 8872 or API :8800 PID 18416. Do not open headed Edge or steal `omb-edge-qa-profile`. Do not run the skeptic session. Do not squash-merge. Do not touch the LiteLLM catalog. #24 stays DRAFT. Logs under `C:\Users\Administrator\grok-runs\omb\`; scrub secrets.
- Owner: grok proxy CLI CoS
- Opened: 2026-09-10
- Status: IN_PROGRESS
- Issue: https://github.com/matthewhand/OpenMausBot/issues/27
- PR path: https://github.com/matthewhand/OpenMausBot/pull/24
