# REQ-009: OpenAI-compatible LiteLLM on .30, .32, and .36

- Intent: All three OpenMausBot instances should offer the LAN LiteLLM (`http://10.0.0.30:8000`) and the Fly LiteLLM (`https://open-litellm.fly.dev/`) as OpenAI-compatible engines.
- Success: Each host’s `/api/instances` lists two openai-compat engines as available (or at least authenticated against a live `/models` list). Existing grok/agy/etc. engines remain. API key is read from ubuntu-max `open-litellm/.env` and is not printed.
- Constraints: Do not paste keys into chat. Do not remove the default CLI fleet. LAN URL is `10.0.0.30:8000` (not container localhost). Restart OMB only as needed to load instances.
- Owner: Engineering
- Opened: 2026-09-03
- Status: DONE
- Closed: 2026-09-03
- Evidence: `litellmLan` (`http://10.0.0.30:8000/v1`, 13 models) and `litellmFly` (`https://open-litellm.fly.dev/v1`, 68 models) are `available` on .30, .32, and .36. Key taken from `~/open-litellm/.env` LITELLM_MASTER_KEY. Default CLI fleet kept.
