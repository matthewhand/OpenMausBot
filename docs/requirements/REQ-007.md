# REQ-007: Model picker puts recent and working CLIs first

- Intent: The model dropdown lists every CLI equally, so recently used and known-working engines are hard to find.
- Success: Cloud and Local rails list recently picked engines first, then other ready engines, then engines that still need install/sign-in. Suggested models pin recent picks for that engine. A new pick is remembered for later visits (localStorage). Existing tests plus unit tests for the ranker cover this.
- Constraints: Keep Cloud vs Local grouping. Do not hide engines. Do not change server model catalogs. Windows and LAN UIs share the same renderer.
- Owner: Engineering
- Opened: 2026-09-03
- Status: DONE
- Closed: 2026-09-03
- Evidence: `rankEngineRail` and `suggestedModels` unit tests pass. Picker persists picks in `openmausbot.modelPickerRecent.v1`, seeds from workspace bot selections, and badges recent engines/models.
