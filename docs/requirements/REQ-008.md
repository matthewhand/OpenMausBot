# REQ-008: Copy Windows review chats onto ubuntu-max bots

- Intent: The max instance has the same agent names but empty threads; copy the Windows review transcripts onto those bots.
- Success: Each Windows bot name that exists on max has the same task titles and approximately the same text-message counts. Max CoS is no longer a 1-message seed thread. Resume cursors, Windows cwd paths, and API keys are not copied. Windows OpenMausBot stays up. No UI code changes.
- Constraints: Map by bot name. Do not copy `resumeCursors`, `cwd`, or `config.json`. Stop the max container only while rewriting its `messages.db` / `bots.json`. Hidden bots included.
- Owner: Engineering
- Opened: 2026-09-03
- Status: DONE
- Closed: 2026-09-03
- Evidence: 16 Windows bots copied by name onto max. CoS 929 messages + extra task "New task". Resume cursors and Windows cwd not copied. Windows instance remained running during snapshot.
