# REQ-006: One max bot per available CLI, each can reply

- Intent: Prove ubuntu-max OpenMausBot can actually run a turn on each installed engine CLI, not just list them as available.
- Success: One new bot per available CLI (grok, claude, codex, antigravity, pi — skip any still unavailable). Each receives a user message and returns a settled bot text reply. Windows OpenMausBot is untouched.
- Constraints: ubuntu-max only (`http://10.0.0.30:8802`). Do not copy keys or transcripts from Windows. Short probe prompts. Do not bind extra ports.
- Owner: Engineering
- Opened: 2026-09-03
- Status: IN_PROGRESS
