# REQ-004: ubuntu-max OpenMausBot without LAN auth (dev)

- Intent: The ubuntu-max Docker OpenMausBot is a local-LAN dev instance; it should start and be usable without a login/token prompt.
- Success: After recreate, `GET http://10.0.0.30:8799/api/health` from this Windows LAN host returns `authRequired: false`; `GET /api/bots` without Authorization succeeds (not 401). Windows OpenMausBot is untouched. Production compose (`docker-compose.yml`) still documents a token for public bind.
- Constraints: ubuntu-max only. Keep Node 24 image, home mount, port 8799 (not LiteLLM 8000). Do not print or copy tokens. Do not disable auth on the Windows review instance.
- Owner: Engineering
- Opened: 2026-09-03
- Status: DONE
- Closed: 2026-09-03
- Evidence: Recreated `deploy-omb-1` with empty `OMB_AUTH_TOKEN`. From this Windows host, `GET http://10.0.0.30:8799/api/health` → `authRequired: false`; `GET /api/bots` with no Authorization → 200; UI `/` → 200. Logs: "No OMB_AUTH_TOKEN set". Windows review pid 10284 still up.
- Related: REQ-001 (that item required auth on LAN; this supersedes that constraint for the max dev stack)
