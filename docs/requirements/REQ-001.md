# REQ-001: OpenMausBot on ubuntu-max

- Intent: Run a second OpenMausBot on matthewh's Ubuntu box as a new home-folder project, from the squash-merged fork, with the same agent lineup as this Windows workspace.
- Success: Dev compose on ubuntu-max is up (Node 24 image); host home is mounted (or a subfolder from .env); health answers; this Windows OpenMausBot is untouched. Roster copy is a follow-on after the stack is up.
- Constraints: Target is ubuntu-max only. Docker Compose, Node 24 in the image. Do not bind over the existing LiteLLM port. Auth on if it listens on the LAN. Do not copy API keys or transcripts. Home mount path overridable via OMB_DEV_HOME.
- Owner: Chief of Staff
- Opened: 2026-09-02
- Status: DONE
- Closed: 2026-09-03
- Evidence: ubuntu-max `deploy-omb-1` healthy on 0.0.0.0:8799; image `openmausbot:dev` Node v24.20.0; `/home/matthewh` bind-mounted at `/host-home`; `GET /api/health` returns openmausbot with auth required; Windows review instance untouched. Roster copy remains a follow-on. CoS re-verified live on 2026-09-03: compose up and healthy, Node 24 image, home mounted, health answering, LAN auth on, not on the LiteLLM port, Windows instance untouched. Agent lineup still not copied (follow-on).
