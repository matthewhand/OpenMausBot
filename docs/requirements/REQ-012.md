# REQ-012: Restore conversation message submit

- Intent: The conversation UI no longer lets the user submit a chat message; restore send (button and Enter) so a typed message actually leaves the composer.
- Success:
  1. In an isolated review/fixture UI (not the user's live `~/.openmausbot` data), a user can type in the composer and submit via the send control and via Enter.
  2. The submitted text appears as a user bubble in the transcript (or a visible queued-send chip if the bot is already busy).
  3. Visual proof: headed screenshots of the composer with typed text, then the transcript after submit.
- Constraints: Do not mutate the user's live OpenMausBot app or `~/.openmausbot`. Prefer the existing review harness (`REVIEW-RUNNING.txt`) or `docs/verification/README.md` isolated fixture. Do not kill live Vite :8802 / API :8800. Visual proof required.
- Owner: Engineering
- Opened: 2026-09-12
- Status: DONE
- Issue: https://github.com/matthewhand/OpenMausBot/issues/31
- Evidence: Isolated review UI (`http://127.0.0.1:8802`, `.review-data`). Overlay clip now `pointer-events-none`. Composer send click and Enter both leave a user bubble. Screenshots: `docs/screenshots/composer-submit-probe/09-typed-after-fix.png`, `docs/screenshots/composer-submit-probe/10-after-send-fix.png`.
